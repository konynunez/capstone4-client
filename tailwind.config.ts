/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Enable dark mode using the "class" strategy
  content: [
    "./src/**/*.{js,ts,jsx,tsx}", // Scan all files inside src directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
